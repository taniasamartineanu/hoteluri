@page
@model hoteluri.Pages.Camere.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Camera</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Camera.CameraId" />
            <div class="form-group">
                <label asp-for="Camera.HotelId" class="control-label"></label>
                <select asp-for="Camera.HotelId" class="form-control" asp-items="ViewBag.HotelId"></select>
                <span asp-validation-for="Camera.HotelId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Camera.NumarCamera" class="control-label"></label>
                <input asp-for="Camera.NumarCamera" class="form-control" />
                <span asp-validation-for="Camera.NumarCamera" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
